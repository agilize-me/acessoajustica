<h2>Editar Extensão</h2>

<%= render 'form' %>

<%= link_to 'Ver detalhes', @tenant %> |
<%= link_to t('.cancel', :default => t("helpers.links.cancel")),
          tenants_path, :class => 'btn btn-default' %>
